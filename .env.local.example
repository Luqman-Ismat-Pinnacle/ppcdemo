# PPC Local Development - Copy to .env.local and adjust
# Required for local dev with database

# Auth bypass (skip Auth0 login)
NEXT_PUBLIC_AUTH_DISABLED=true

# PostgreSQL - use Docker postgres or local install
# Docker: postgresql://postgres:postgres@localhost:5432/ppcdb?sslmode=disable
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ppcdb?sslmode=disable

# Optional: MPP Parser (for .mpp file uploads)
# NEXT_PUBLIC_MPP_PARSER_URL=http://localhost:5001
