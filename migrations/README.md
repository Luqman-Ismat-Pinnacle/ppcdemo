# Legacy migrations folder

Database schema is consolidated in the project root. Use:

- **`/schema.sql`** – Single complete schema (run for clean install or reset).

Supabase migrations live in **`supabase/migrations/`**.
